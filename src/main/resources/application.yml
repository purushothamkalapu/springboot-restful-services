spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_management
    username: root
    password: Krithik2020

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true

info:
  app:
    name: "Spring boot Restful web Services"
    description: "Springboot Restful web service Demo"
    version: "1.0.0"
